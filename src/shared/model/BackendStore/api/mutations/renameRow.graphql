mutation RenameRowMst($data: RenameRowInput!) {
  renameRow(data: $data) {
    table {
      ...TableMst
    }
    previousVersionTableId
    row {
      ...RowMst
    }
    previousVersionRowId
  }
}
