fragment User on UserModel {
  id
  username
  email
  organizationId
  role {
    id
    name
    permissions {
      id
      action
      subject
      condition
    }
  }
}

query getMe {
  me {
    ...User
  }
}
