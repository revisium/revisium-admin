query configuration {
  configuration {
    availableEmailSignUp
    google {
      available
      clientId
    }
    github {
      available
      clientId
    }
    plugins {
      file
      formula
    }
  }
}
