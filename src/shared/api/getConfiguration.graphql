query configuration {
  configuration {
    availableEmailSignUp
    noAuth
    google {
      available
      clientId
    }
    github {
      available
      clientId
    }
    plugins {
      file
      formula
    }
  }
}
