query configuration {
  configuration {
    availableEmailSignUp
    noAuth
    billing {
      enabled
    }
    google {
      available
      clientId
    }
    github {
      available
      clientId
    }
    cache {
      enabled
    }
    plugins {
      file
    }
  }
}
